import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { Autocomplete, TextField } from '../../../src'
import { listOfCountries } from '../../util/data'

<Meta title="Labs/Autocomplete" component={Autocomplete} />

# ⚠ Autocomplete

Re-exports [Material UI Lab Autocomplete](https://material-ui.com/components/autocomplete/).
To use you must also have the peer dependency `@material-ui/lab` installed.

<Canvas>
  <Story name="default">
    {() => {
      return (
        <Autocomplete
          debug
          id="combo-box-demo"
          options={listOfCountries}
          getOptionLabel={(option) => option.title}
          style={{ width: 300 }}
          renderInput={(params) => <TextField {...params} label="Combo box" />}
        />
      )
    }}
  </Story>
</Canvas>

# Props

Supports standard html aria and event props for a `div`.

## Main props

<ArgsTable of={Autocomplete} />
