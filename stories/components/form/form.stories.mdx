import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { Form, TextField, Button } from '../../../src'
import { FormDocs } from './FormDocs'

<Meta title="Components/Form" component={Form} />

# Form

Extends html `form` to add positioning props.

<Canvas>
  <Story name="default">
    <Form width={1 / 2}>
      <TextField id="standard-name" label="Name" margin="normal" />
      <Button color="primary" mt={2}>
        Submit
      </Button>
    </Form>
  </Story>
</Canvas>

# Props

Supports all usual form props and [Positioning props](/components/?path=/docs/design-system-positioning--page).

<ArgsTable of={FormDocs} />
