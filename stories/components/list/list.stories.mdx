import { useState } from 'react'
import { action } from '@storybook/addon-actions'
import {
  Divider,
  List,
  Flex,
  Text,
  ListItem,
  ListItemLink,
  Checkbox,
  IconButton,
  Box,
  ListItemIcon,
  ListItemText,
  ListItemSecondaryAction,
} from '../../../src'
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import {
  ListDocs,
  ListItemDocs,
  ListItemAvatarDocs,
  ListItemIconDocs,
  ListItemSecondaryActionDocs,
  ListItemTextDocs,
  ListSubheaderDocs,
} from './ListDocs'
import { Icons } from '../../util/Icons'

<Meta title="Components/List" component={List} />

# List

Extends [Material-UI List](https://material-ui.com/components/lists/) and associated sub-components.

Lists can be used as toggle actions or as part of input forms.
Use when single select/deselect option, use radio when one option of more then two choices; [further info](https://www.nngroup.com/articles/checkboxes-vs-radio-buttons/).

<Canvas>
  <Story name="default">
    <Flex justifyContent="center" bgcolor="background.default">
      <Box minWidth={500} m={3} bgcolor="background.paper">
        <List component="nav" aria-label="main mailbox folders">
          <ListItem button>
            <ListItemIcon>
              <Icons.Inbox />
            </ListItemIcon>
            <ListItemText primary="Inbox" />
          </ListItem>
          <ListItem button>
            <ListItemIcon>
              <Icons.Drafts />
            </ListItemIcon>
            <ListItemText primary="Drafts" />
          </ListItem>
        </List>
        <Divider />
        <List component="nav" aria-label="secondary mailbox folders">
          <ListItem button>
            <ListItemText primary="Trash" />
          </ListItem>
          <ListItem button>
            <ListItemText primary="Spam" />
          </ListItem>
        </List>
      </Box>
    </Flex>
  </Story>
</Canvas>

<Canvas>
  <Story name="with icons">
    <Flex justifyContent="center" bgcolor="background.default">
      <Box minWidth={500} m={3} bgcolor="background.paper">
        <List>
          <ListItem role={undefined} button onClick={action('clicked item')}>
            <ListItemIcon>
              <Checkbox
                edge="start"
                color="primary"
                tabIndex={-1}
                disableRipple
                inputProps={{ 'aria-labelledby': 1 }}
                onClick={action('clicked checkbox')}
              />
            </ListItemIcon>
            <ListItemText id={1} primary={`Line item`} />
            <ListItemSecondaryAction onClick={action('clicked action')}>
              <IconButton edge="end" aria-label="comments">
                <Icons.Comment />
              </IconButton>
            </ListItemSecondaryAction>
          </ListItem>
        </List>
      </Box>
    </Flex>
  </Story>
</Canvas>

## To Fix

Secondary text is not being displayed properly

<Canvas>
  <Story name="with secondary">
    <Flex justifyContent="center" bgcolor="background.default">
      <Box minWidth={500} m={3} bgcolor="background.paper">
        <List>
          <ListItem>
            <ListItemText primary="Item" secondary="secondary" />
          </ListItem>
        </List>
      </Box>
    </Flex>
  </Story>
</Canvas>

## With Labels

## Props

## List

<ArgsTable of={ListDocs} />

## ListItem

<ArgsTable of={ListItemDocs} />

## ListItemAvatar

<ArgsTable of={ListItemAvatarDocs} />

## ListItemIcon

<ArgsTable of={ListItemIconDocs} />

## ListItemSecondaryAction

<ArgsTable of={ListItemSecondaryActionDocs} />

## ListItemText

<ArgsTable of={ListItemTextDocs} />

## ListSubheader

<ArgsTable of={ListSubheaderDocs} />
