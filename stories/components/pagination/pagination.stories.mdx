import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { Skeleton, Card, CardHeader } from '../../../src'

<Meta title="Labs/Skeleton" component={Skeleton} />

# ⚠ Skeleton

Re-exports [Material UI Lab Skeleton](https://material-ui.com/components/skeleton/).
To use you must also have the peer dependency `@material-ui/lab` installed.

<Canvas>
  <Story name="default">
    <Card>
      <CardHeader
        avatar={<Skeleton variant="circle" width={40} height={40} />}
        title={<Skeleton variant="text" />}
      />
      <Skeleton variant="rect" height={118} />
    </Card>
  </Story>
</Canvas>

# Props

## Main props

<ArgsTable of={Skeleton} />
