import { Icons, Flex } from '../../../src'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { IconsDocs } from './IconsDocs'
import { Copy } from '../../util/copy'

<Meta title="Components|Icons" component={Icons} />

# Icon

Extends [Material-UI Icons](https://material-ui.com/components/icons/).

<Preview>
  <Story name="default">
    <Icons.Mood />
  </Story>
</Preview>

We use the Sharp set of icons from Material (but drop the sharp from the name title).
Click to copy the name. Then use `<Icons.Name />`

<Preview>
  <Story name="icons">
    {React.createElement(() => {
      const icons = Object.keys(Icons)
      return (
        <Flex flexWrap="wrap">
          {icons.map((key) => (
            <Copy m={1} key={key} title={key}>
              {React.createElement(Icons[key])}
            </Copy>
          ))}
        </Flex>
      )
    })}
  </Story>
</Preview>

## Props

Supports standard `svg` props.

<Props of={IconsDocs} />
