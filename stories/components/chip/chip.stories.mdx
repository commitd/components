import { Chip, Flex, Avatar, Icons } from '../../../src'
import image from '../../../public/images/Avatar1-YellowTrans-32px.png'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'
import { ChipDocs } from './ChipDocs'

<Meta title="components|Chip" component={Chip} />

# Chip

Extends [Material UI Chip](https://material-ui.com/components/chips/).

Chips can be used for small bits of information such as labels or attributes and can optionally
add actions to, say edit and delete.

<Preview>
  <Story name="default">
    <Chip label="Basic Chip" />
  </Story>
  <Story name="Clickable Chip">
    <Chip
      avatar={<Avatar>MB</Avatar>}
      label="Clickable Chip"
      onClick={action('clicked')}
    />
  </Story>
  <Story name="Deletable Chip">
    <Chip
      avatar={<Avatar alt="Natacha" src={image} />}
      label="Deletable Chip"
      onDelete={action('deleted')}
    />
  </Story>
  <Story name="With Icon">
    <Chip
      avatar={
        <Avatar>
          <Icons.Face />
        </Avatar>
      }
      label="Clickable Deletable Chip"
      onClick={action('clicked')}
      onDelete={action('deleted')}
    />
  </Story>
  <Story name="With Icon and Delete">
    <Chip
      icon={<Icons.Face />}
      label="Icon Deletable Chip"
      clickable={true}
      onClick={action('clicked')}
      onDelete={action('deleted')}
    />
  </Story>
  <Story name="Custom delete icon Chip">
    <Chip
      label="Custom delete icon Chip"
      onClick={action('clicked')}
      onDelete={action('deleted')}
      deleteIcon={<Icons.Done />}
    />
  </Story>
  <Story name="Clickable Link Chip">
    <Chip label="Clickable Link Chip" component="a" href="#chip" clickable />
  </Story>
  <Story name="Other">
    <Chip
      avatar={<Avatar>MB</Avatar>}
      label="Primary Clickable Chip"
      clickable={true}
      color="primary"
      onClick={action('clicked')}
      onDelete={action('deleted')}
      deleteIcon={<Icons.Done />}
    />
  </Story>
  <Story name="Deletable Primary Chip">
    <Chip
      label="Deletable Primary Chip"
      onDelete={action('deleted')}
      color="primary"
    />
  </Story>
  <Story name="Deletable Secondary Chip">
    <Chip
      avatar={
        <Avatar>
          <Icons.Face />
        </Avatar>
      }
      label="Deletable Secondary Chip"
      onDelete={action('deleted')}
      color="secondary"
    />
  </Story>
  <Story name="Deletable Secondary Chip With Icon">
    <Chip
      icon={<Icons.Face />}
      label="Deletable Secondary Chip"
      onDelete={action('deleted')}
      color="secondary"
    />
  </Story>
</Preview>

# Outlined

There is also an outlined variant

<Preview>
  <Story name="Outlined default">
    <Chip
      variant="outlined"
      icon={<Icons.Face />}
      label="Chip"
      onClick={action('clicked')}
      onDelete={action('deleted')}
      color="default"
    />
  </Story>
  <Story name="Outlined primary">
    <Chip
      variant="outlined"
      icon={<Icons.Face />}
      label="Chip"
      onClick={action('clicked')}
      onDelete={action('deleted')}
      color="primary"
    />
  </Story>
  <Story name="Outlined secondary">
    <Chip
      variant="outlined"
      icon={<Icons.Face />}
      label="Chip"
      onClick={action('clicked')}
      onDelete={action('deleted')}
      color="secondary"
    />
  </Story>
</Preview>

# Size

There is also a small size

<Preview>
  <Story name="small default">
    <Chip
      size="small"
      icon={<Icons.Face />}
      label="Chip"
      onClick={action('clicked')}
      onDelete={action('deleted')}
      color="default"
    />
  </Story>
  <Story name="small outlined">
    <Chip
      size="small"
      variant="outlined"
      icon={<Icons.Face />}
      label="Chip"
      onClick={action('clicked')}
      onDelete={action('deleted')}
      color="default"
    />
  </Story>
</Preview>

## Props

Supports Spacing and Flexbox [Positioning props](/components/?path=/docs/design-system-positioning--page).
Supports Aria amd standard `'div'` props.

<Props of={ChipDocs} />
