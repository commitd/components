import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { Tooltip, IconButton, Button, Icons, Flex, Box } from '../../../src'
import { TooltipDocs } from './TooltipDocs'

<Meta title="components|Tooltip" component={Tooltip} />

# Tooltip

Extends [Material UI Tooltip](https://material-ui.com/components/tooltips/)

Tooltips display extra information when users hover over, focus on, or tap an element.

When activated, Tooltips display a text label identifying an element, such as a description of its function.

<Preview>
  <Story name="default">
    <Flex justifyContent="center">
      <Tooltip title="Delete">
        <IconButton aria-label="delete">
          <Icons.Delete />
        </IconButton>
      </Tooltip>
    </Flex>
  </Story>
</Preview>

## Placement

The Tooltip has 12 placements choices. Prefer, Top, Bottom, Right, Left, in that order.

<Preview>
  <Story name="placement">
    <div>
      <Flex justifyContent="center">
        <Tooltip title="Add" placement="top-start">
          <Button m={2}>top-start</Button>
        </Tooltip>
        <Tooltip title="Add" placement="top">
          <Button m={2}>top</Button>
        </Tooltip>
        <Tooltip title="Add" placement="top-end">
          <Button m={2}>top-end</Button>
        </Tooltip>
      </Flex>
      <Flex justifyContent="center">
        <Flex alignItems="flex-start" flexDirection="column">
          <Tooltip title="Add" placement="left-start">
            <Button m={2}>left-start</Button>
          </Tooltip>
          <Tooltip title="Add" placement="left">
            <Button m={2}>left</Button>
          </Tooltip>
          <Tooltip title="Add" placement="left-end">
            <Button m={2}>left-end</Button>
          </Tooltip>
        </Flex>
        <Box width={200} />
        <Flex alignItems="flex-end" flexDirection="column">
          <Tooltip title="Add" placement="right-start">
            <Button m={2}>right-start</Button>
          </Tooltip>
          <Tooltip title="Add" placement="right">
            <Button m={2}>right</Button>
          </Tooltip>
          <Tooltip title="Add" placement="right-end">
            <Button m={2}>right-end</Button>
          </Tooltip>
        </Flex>
      </Flex>
      <Flex justifyContent="center">
        <Tooltip title="Add" placement="bottom-start">
          <Button m={2}>bottom-start</Button>
        </Tooltip>
        <Tooltip title="Add" placement="bottom">
          <Button m={2}>bottom</Button>
        </Tooltip>
        <Tooltip title="Add" placement="bottom-end">
          <Button m={2}>bottom-end</Button>
        </Tooltip>
      </Flex>
    </div>
  </Story>
</Preview>

# Props

<Props of={TooltipDocs} />
