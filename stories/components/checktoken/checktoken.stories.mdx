import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { CheckToken, ToggleButtonGroup } from '../../../src'

<Meta title="Labs/CheckToken" component={CheckToken} />

# ⚠ Check Token

To use you must also have the peer dependency `@material-ui/lab` installed.
this component is a work in progress and should improve with the next material release.

# Check Tokens

<Canvas>
  <Story name="default">
    {() => {
      const [selected, setSelected] = React.useState(false)
      return (
        <CheckToken
          aria-label="check-token"
          selected={selected}
          onClick={() => {
            setSelected(!selected)
          }}
        >
          Label
        </CheckToken>
      )
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="colors">
    {() => {
      const [selected, setSelected] = React.useState(false)
      return (
        <>
          <CheckToken
            selected={selected}
            onClick={() => setSelected(!selected)}
          >
            Default
          </CheckToken>
          <CheckToken
            color="primary"
            selected={selected}
            onClick={() => setSelected(!selected)}
          >
            Primary
          </CheckToken>
          <CheckToken
            color="secondary"
            selected={selected}
            onClick={() => setSelected(!selected)}
          >
            Secondary
          </CheckToken>
        </>
      )
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="controller group">
    {() => {
      const [value, setValue] = React.useState('')
      return (
        <>
          <CheckToken
            color="primary"
            selected={'one' === value}
            onClick={() => setValue('one')}
          >
            One
          </CheckToken>
          <CheckToken
            color="primary"
            selected={'two' === value}
            onClick={() => setValue('two')}
          >
            Two
          </CheckToken>
          <CheckToken
            color="primary"
            selected={'three' === value}
            onClick={() => setValue('three')}
          >
            Three
          </CheckToken>
          <CheckToken
            color="primary"
            selected={'four' === value}
            disabled
            onClick={() => setValue('four')}
          >
            Four
          </CheckToken>
        </>
      )
    }}
  </Story>
</Canvas>

# Props

Supports standard aria and event props for a `div`.

## Main props

<ArgsTable of={CheckToken} />
