import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { Button, Container } from '../../../src'
import { PageHeading } from './PageHeading'

<Meta title="Components/PageHeading" component={PageHeading} />

# PageHeading

Lays out buttons (or other components) together. Intended to be used at the top of a page after the page heading.

<Canvas>
  <Story name="default">
    <Container maxWidth="md">
      <PageHeading
        title="Users"
        buttons={[
          <Button color="primary">Add</Button>,
          <Button color="primary">Export</Button>,
          <Button color="primary">Delete</Button>,
        ]}
      />
    </Container>
  </Story>
</Canvas>

## Props

<ArgsTable of={PageHeading} />
