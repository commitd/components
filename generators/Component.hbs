import React from 'react'
import { styled } from 'stitches.config'


export const _{{ name }} = styled('div', {})


type Styled{{ name }}Props = React.ComponentProps<typeof _{{ name }}>
export type {{ name }}Props = Styled{{ name }}Props & {}

/**
* {{ name }} component
*/
export const {{ name }}: React.FC<{{ name }}Props> = _{{ name }}
