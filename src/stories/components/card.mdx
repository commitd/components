import {
  Card,
  CardHeader,
  CardContent,
  CardActionArea,
  CardActions,
  CardMedia,
  Button
} from '../../'
import { Cartesian } from '@compositor/kit'
import bgImage from '../../../public/images/Committed-Colour-512px.png'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'

<Meta title="components|Card" component={Card} />

# Card

Use `Card` component to group element onto a card.

<Preview>
  <Story name="default">
    <Card>
      <CardHeader>Simple Card</CardHeader>
      <CardContent>The contents of the card</CardContent>
    </Card>
  </Story>
</Preview>

## Card Variations

<Preview>
  <Story name="square">
    <Card m={4} square={true}>
      <CardHeader>Square</CardHeader>
    </Card>
  </Story>
  <Story name="image">
    <Card m={4}>
      <CardMedia image={bgImage} title="Committed image">
        <CardHeader>Media</CardHeader>
        <CardContent>This is the content</CardContent>
      </CardMedia>
    </Card>
  </Story>
  <Story name="raised">
    <Card m={4} raised={true}>
      <CardHeader>Raised</CardHeader>
    </Card>
  </Story>
</Preview>

## Card Header

<Preview>
  <Story name="no header">
    <Card m={4} width={[1, 1 / 2]}>
      <CardContent>No header</CardContent>
    </Card>
  </Story>
  <Story name="header as child">
    <Card m={4} width={[1, 1 / 2]}>
      <CardHeader>As child</CardHeader>
      <CardContent>Content</CardContent>
    </Card>
  </Story>
  <Story name="header as title">
    <Card m={4} width={[1, 1 / 2]}>
      <CardHeader title="header as title" />
      <CardContent>Content</CardContent>
    </Card>
  </Story>
  <Story name="header and subheader">
    <Card m={4} width={[1, 1 / 2]}>
      <CardHeader title="Title" subheader="and subheader" />
      <CardContent>Content</CardContent>
    </Card>
  </Story>
  <Story name="header with avatar">
    <Card m={4} width={[1, 1 / 2]}>
      <CardHeader avatar={'A'} title="Avatar" />
      <CardContent>Content</CardContent>
    </Card>
  </Story>
</Preview>

## Card Actions

<Preview>
  <Story name="action area">
    <Card m={4} width={[1, null, 1 / 3]}>
      <CardHeader>Action Area</CardHeader>
      <CardContent>An action ares can be at the bottom of the card</CardContent>
      <CardActionArea onClick={action('action')}>
        <h1 style={{ textAlign: 'center' }}>Action</h1>
      </CardActionArea>
    </Card>
  </Story>
  <Story name="action buttons">
    <Card m={4} width={[1, null, 1 / 3]}>
      <CardHeader>Actions</CardHeader>
      <CardContent>Actions can be added to the card</CardContent>
      <CardActions>
        <Button color={'default'} variant={'text'} onClick={action('cancel')}>
          Cancel
        </Button>
        <Button color={'primary'} onClick={action('accept')}>
          Accept
        </Button>
      </CardActions>
    </Card>
  </Story>
  <Story name="action full">
    <Card m={4} width={[1, null, 1 / 3]}>
      <CardActionArea onClick={action('full')}>
        <CardMedia height="120px" image={bgImage} title="Committed image" />
        <CardHeader>Full Card Action</CardHeader>
        <CardContent>
          <p>The whole card can be an action area</p>
        </CardContent>
      </CardActionArea>
    </Card>
  </Story>
</Preview>

## Elevation

<Preview>
  <Cartesian
    component={Card}
    margin={4}
    elevation={[0, 1, 2, 3, 4, 6]}
    children={<CardContent>Contents</CardContent>}
  />
</Preview>

## Props

Main props for cards, excluding normal spacing props, see [Box](/components/box).

<Props of={Card} />
<Props of={CardHeader} />
<Props of={CardContent} />
<Props of={CardActionArea} />
<Props of={CardActions} />
<Props of={CardMedia} />
