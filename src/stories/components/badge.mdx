import { Badge, Button, Text, Flex, Box, Heading } from "@components"
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks"

<Meta title="components|Badge" component={Badge} />

# Badge

<Preview>
  <Story name="default">
    <Badge badgeContent={"text"}>
      <Text>Typography</Text>
    </Badge>
  </Story>
  <Story name="primary">
    <Badge color="primary" badgeContent={4}>
      <Heading.h3>Heading</Heading.h3>
    </Badge>
  </Story>
  <Story name="secondary">
    <Badge color="secondary" badgeContent={100}>
      <Button variant="contained">Button</Button>
    </Badge>
  </Story>
  <Story name="error">
    <Badge color="error" badgeContent={1000}>
      <Button variant="contained">Error</Button>
    </Badge>
  </Story>
</Preview>

## Props

<Props of={Badge} />
