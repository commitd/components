import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'
import {
  Tabs,
  Tab,
  TabPanel,
  IconButton,
  Button,
  Icons,
  Box,
  AppBar
} from '../../'
import { TabDocs } from './Tab'
import { TabsDocs } from './Tabs'
import { TabPanelDocs } from './TabPanel'

<Meta title="components|Tabs" component={Tabs} />

# Tabs

Extends [Material UI Tab](https://material-ui.com/components/tabs/)

Tabs can be used for navigation between different components at the same level of heirarchy.
In addition to the `Tab` extenaion we add a `TabPanel` component for convenience, it's use is shown below.

<Preview>
  <Story name="appbar">
    {() => {
      const [value, setValue] = React.useState(0)
      function a11yProps(index) {
        return {
          id: `simple-tab-${index}`,
          'aria-controls': `simple-tabpanel-${index}`
        }
      }
      const clicked = action('clicked')
      function handleChange(event, newValue) {
        setValue(newValue)
        clicked(newValue)
      }
      return (
        <Box>
          <AppBar position="static">
            <Tabs
              value={value}
              onChange={handleChange}
              aria-label="simple tabs example"
            >
              <Tab label="Item One" {...a11yProps(0)} />
              <Tab label="Item Two" {...a11yProps(1)} />
              <Tab label="Item Three" {...a11yProps(2)} />
            </Tabs>
          </AppBar>
          <TabPanel p={3} selected={value} index={0}>
            Item One
          </TabPanel>
          <TabPanel p={3} selected={value} index={1}>
            Item Two
          </TabPanel>
          <TabPanel p={3} selected={value} index={2}>
            Item Three
          </TabPanel>
        </Box>
      )
    }}
  </Story>
</Preview>

## Simple

<Preview>
  <Story name="default">
    {() => {
      const [value, setValue] = React.useState(0)
      const clicked = action('clicked')
      function handleChange(event, newValue) {
        setValue(newValue)
        clicked(newValue)
      }
      return (
        <Box>
          <Tabs value={value} onChange={handleChange} centered>
            <Tab label="Item One" />
            <Tab label="Item Two" />
            <Tab label="Item Three" />
          </Tabs>
        </Box>
      )
    }}
  </Story>
</Preview>

# Props

## Tabs

<Props of={TabsDocs} />

## Tab

<Props of={TabDocs} />

## TabPanel

Supports all [Box](/?path=/docs/components-box--default-story) and [Positioning props](/?path=/docs/design-system-positioning--page)

<Props of={TabPanelDocs} />
