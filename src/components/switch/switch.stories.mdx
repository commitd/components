import { Switch, Form, FormGroup, FormControlLabel, FormLabel } from '../../'
import { action } from '@storybook/addon-actions'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { SwitchDocs } from './Switch'

<Meta title="components|Switch" component={Switch} />

# Switch

To toggle states

<Preview>
  <Story name="default">
    <Switch
      defaultChecked
      value="checkedF"
      color="default"
      inputProps={{ 'aria-label': 'checkbox with default color' }}
    />
  </Story>
  <Story name="primary">
    <Switch
      onChange={action('checkedB')}
      value="checkedB"
      color="primary"
      inputProps={{ 'aria-label': 'primary checkbox' }}
    />
  </Story>
  <Story name="secondary">
    <Switch
      onChange={action('checkedA')}
      value="checkedA"
      inputProps={{ 'aria-label': 'secondary checkbox' }}
    />
  </Story>
  <Story name="controlled">
    <Switch value="true" />
  </Story>
  <Story name="disabled">
    <Switch
      disabled={true}
      value="checkedD"
      inputProps={{ 'aria-label': 'disabled checkbox' }}
    />
  </Story>
  <Story name="checked">
    <Switch disabled={true} checked={true} />
  </Story>
</Preview>

## Labels

<Preview>
  <Story name="labels">
    {() => {
      const [checked, setChecked] = React.useState(true)
      const handleChange = event => setChecked(!checked)
      return (
        <Form>
          <FormLabel component="legend">Switches</FormLabel>
          <FormGroup row>
            <FormControlLabel
              control={
                <Switch
                  checked={checked}
                  onChange={handleChange}
                  color="primary"
                />
              }
              label="Primary"
            />
            <FormControlLabel
              control={
                <Switch
                  checked={checked}
                  onChange={handleChange}
                  color="secondary"
                />
              }
              label="Secondary"
            />
            <FormControlLabel
              control={
                <Switch
                  checked={checked}
                  onChange={handleChange}
                  color="default"
                />
              }
              label="Default"
            />
          </FormGroup>
          <FormLabel component="legend">Disabled</FormLabel>
          <FormGroup row>
            <FormControlLabel
              control={
                <Switch
                  checked={checked}
                  onChange={handleChange}
                  color="primary"
                />
              }
              disabled={true}
              label="Primary"
            />
            <FormControlLabel
              control={
                <Switch
                  checked={checked}
                  onChange={handleChange}
                  color="secondary"
                />
              }
              disabled={true}
              label="Secondary"
            />
            <FormControlLabel
              control={
                <Switch
                  checked={checked}
                  onChange={handleChange}
                  color="default"
                />
              }
              disabled={true}
              label="Default"
            />
          </FormGroup>
        </Form>
      )
    }}
  </Story>
</Preview>

## Props

Supports [Display, Spacing and Flexbox props](/components/?path=/docs/design-system-positioning--page).

<Props of={SwitchDocs} />
