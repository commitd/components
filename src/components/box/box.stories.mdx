import { Box } from '../../'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { BaseBoxDocs, RestBoxDocs } from './Box'

<Meta title="components|Box" component={Box} />

# Box

Extends [Material UI Box](https://material-ui.com/components/box/).

Use the `Box` component to control width, margin, padding, and color.

50% width

<Preview>
  <Story name="default">
    <Box width={1 / 2} border={1}>
      Box
    </Box>
  </Story>
</Preview>

Padding of `theme.space[3]` (16px)

<Preview>
  <Story name="padding">
    <Box p={3} border={1}>
      Box
    </Box>
  </Story>
</Preview>

Margin of `theme.space[2]` (8px)

<Preview>
  <Story name="margin">
    <Box p={1} m={4} border={1}>
      Box
    </Box>
  </Story>
</Preview>

Color primary from the theme's color palette

<Preview>
  <Story name="primary">
    <Box p={2} bgcolor="primary.main" color="white">
      Box
    </Box>
  </Story>
</Preview>

## Responsive Widths

The `width` prop accepts an array value to set different widths at different breakpoints with a mobile-first approach.

<Preview>
  <Story name="responsive">
    <Box
      width={[
        1, // Sets width 100% at the smallest breakpoint
        1 / 2, // Sets width 50% at the next breakpoint
        1 / 4 // Sets width 25% at the next breakpoint
      ]}
      p={2}
      bgcolor="secondary.main"
      color="grey.600"
    >
      Hello
    </Box>
  </Story>
</Preview>

## Props

Supports all [Positioning props](/components/?path=/docs/design-system-positioning--page).

## Main props

<Props of={BaseBoxDocs} />

## Other props

<Props of={RestBoxDocs} />
