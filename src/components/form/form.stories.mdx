import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { Form, TextField, Button } from '../../'
import { FormDocs } from './Form'

<Meta title="components|Form" component={Form} />

# Form

Extends html `form` to add positioning props.

<Preview>
  <Story name="default">
    <Form width={1 / 2}>
      <TextField id="standard-name" label="Name" margin="normal" />
      <Button color="primary" mt={2}>
        Submit
      </Button>
    </Form>
  </Story>
</Preview>

# Props

Supports all usual form props and [Positioning props](/?path=/docs/design-system-positioning--page).
