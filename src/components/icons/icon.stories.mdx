import { Icons, Tooltip } from '../../'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

<Meta title="components|Icons" component={Icons} />

# Icon

<Preview>
  <Story name="icons">
    {React.createElement(() => {
      const icons = Object.keys(Icons)
      return (
        <div>
          {icons.map(key => (
            <Tooltip title={key}>{React.createElement(Icons[key])}</Tooltip>
          ))}
        </div>
      )
    })}
  </Story>
</Preview>

## Props

<Props of={Icons.Delete} />
